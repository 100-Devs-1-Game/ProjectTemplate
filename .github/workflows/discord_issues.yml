name: Discord review
on:
  issues:
    types: [opened]

jobs:
  notify-created-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_ISSUES_WEBHOOK }}
        uses: Ilshidur/action-discord@master
        with:
          args: 'Issue created for ${{ github.event.repository.name }}: ${{ github.event.issue.title }} - ${{ github.event.issue.html_url }}'
